condition: self['step']['short_name'] in steps_to_include.get(self['step']['entity_type'], {})

steps_to_include:

    Asset:
        - matte_painting
        - surface
        - texture
        
    Shot:
        - art
        - matte_painting
        - mograph

    # BookPage
    CustomEntity21:
        - colour
        - layout

